// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://avirals554.github.io",
  base: "/heart-ache", // skip this if using a user site (yourusername.github.io repo)
});
